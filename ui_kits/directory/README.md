# Directory Website UI Kit

The BullFrog Toolbelt homepage: dark pond-green header with logo + slogan search, a category filter row of pill tags, a grid of lily-pad ToolCards, and a "browse all" CTA. Built from `Button`, `Tag`, `Input`, `NavBar`, `ToolCard`. Filter tags are clickable (client-side only, no real filtering wired up).
