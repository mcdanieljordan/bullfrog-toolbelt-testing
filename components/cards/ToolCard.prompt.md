The core listing card for a teacher tool in the directory. Three corners rounded generously, bottom-left squared into the brand's signature leaf notch.

```jsx
<ToolCard
  title="Kahoot!"
  description="Turn any lesson into a quiz-show style review game."
  tags={['Free', 'Grades 3-8']}
  cta="Visit Tool"
  onClick={() => {}}
/>
```

Lifts slightly and deepens its shadow on hover. First tag defaults to the accent (sun yellow) treatment for status ("Free"/"New"); the rest use the primary green tint for taxonomy (grade/subject).
