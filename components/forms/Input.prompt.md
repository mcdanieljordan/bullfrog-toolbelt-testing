Pill search field, used at the top of the directory to search tool names.

```jsx
<Input placeholder="Search tools..." value={q} onChange={e => setQ(e.target.value)} />
```

Border switches to the sun-yellow focus ring on focus — never the browser default blue.
