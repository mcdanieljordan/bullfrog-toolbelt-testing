Site header on the pond-deep dark background — the one place that color dominates the page.

```jsx
<NavBar links={['Browse', 'Categories', 'Submit a Tool']} cta="Submit a Tool" />
```

CTA button uses the sun-yellow fill with ink-dark text (best contrast on the dark header).
